# WireGuard Manager - 环境变量配置示例
# =========================================
# 将此文件复制为 .env 并修改相应的值

# Web 管理界面身份认证配置
# -------------------------
# 管理员用户名（默认: admin）
ADMIN_USERNAME=admin

# 管理员密码（必须修改！）
# 建议：至少12位，包含大小写字母、数字和特殊字符
ADMIN_PASSWORD=your_strong_password_here

# Flask SECRET_KEY（用于会话加密）
# 生成随机密钥命令: openssl rand -hex 32
SECRET_KEY=your_random_secret_key_here

# WireGuard 配置
# --------------
# WireGuard 接口名称（默认: wg0）
WG_INTERFACE=wg0

# WireGuard 监听端口（默认: 51820）
WG_PORT=51820

# VPN 内网地址段（默认: 10.8.0.1/24）
SERVER_VPN_IP=10.8.0.1/24

# Web 服务配置
# ------------
# Web 管理界面端口（默认: 8080）
WEB_PORT=8080

# 时区设置（默认: Asia/Shanghai）
TZ=Asia/Shanghai

# 安全建议
# --------
# 1. 立即修改 ADMIN_PASSWORD 为强密码
# 2. 生成随机的 SECRET_KEY
# 3. 不要将此文件提交到版本控制系统
# 4. 在生产环境使用 HTTPS（配置 nginx 反向代理）
# 5. 定期更换密码和密钥
